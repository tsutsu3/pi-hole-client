package io.github.tsutsu3.pi_hole_client.widget

/**
 * Release build configuration for widget logging.
 *
 * This file is only included in release builds.
 * Logging is DISABLED in release builds to prevent sensitive information leakage.
 */
object WidgetDebugConfig {
    const val DEBUG = false
}
